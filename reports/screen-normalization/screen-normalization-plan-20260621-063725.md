# ENGREMIAT Screen Normalization Plan

status: PREPARED
source: 
C:\ENGREMIAT_CORE\reports\screen-normalization\screen-inventory-fast-20260621-063703.md

## Norma comun
[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar

## Regla de seguridad
- No reescribir pantallas funcionales si basta parche minimo.
- Una pantalla por bloque.
- Siempre backup + parse + smoke + rollback.
- Sin Git hasta cierre de fase.

## Orden recomendado
1. ENGREMIAT.ps1 — ya funcional; solo verificar.
2. tools\human-card-assistant.ps1 — verificar leyenda y b/q.
3. tools\data-intake.ps1 — verificar existencia, leyenda y tarjeta observable.
4. tools\health\health-launchers.ps1 — verificar health y salida.
5. tools\launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1 — normalizar con maxima cautela, porque es el launcher operativo real.

## Inventario actual

```text
# ENGREMIAT Screen Inventory Fast

status: PASS
files: 5

## ENGREMIAT.ps1
- parse_ok: True
- has_common_legend: True
- legends:
  - function Wait-Back(){ W ""; $x = Read-Host "Enter = volver/refrescar | b/q = salir"; $x = ($x + "").Trim().ToLowerInvariant(); if($x -eq "b" -or $x -eq "q"){ return "quit" }; return "back" }
  - function Show-Menu(){ Clear-Host; W "==== ENGREMIAT / LAUNCHER HUMANO ESTABLE ====" Cyan; W ""; W "Ruta: $PSCommandPath" DarkGray; W "Principio: este archivo no cambia de nombre; enruta a launchers versionados" DarkGray; W ""; W "[1] Inicio operador" White; W "[2] Proyectos" White; W "[3] Memoria y documentacion" White; W "[4] Data Intake" White; W "[5] Health launchers" White; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W "" }
  - function Show-Help(){ W "AYUDA" Cyan; W ""; W "1 = abre el launcher operativo principal detectado." White; W "2 = abre pantalla/launcher de proyectos detectado." White; W "3 = abre memoria y documentacion si existe." White; W "4 = abre Data Intake si existe." White; W "5 = ejecuta diagnostico de launchers si existe." White; W "m = abre asistente de tarjetas humanas/mantenimiento." White; W "Enter vacio = refrescar." White; W "b/q = salir." White; $r=Wait-Back; if($r -eq "quit"){ throw "__ENGREMIAT_QUIT__" } }
- prompts:
  - function Wait-Back(){ W ""; $x = Read-Host "Enter = volver/refrescar | b/q = salir"; $x = ($x + "").Trim().ToLowerInvariant(); if($x -eq "b" -or $x -eq "q"){ return "quit" }; return "back" }
  - while($true){ try{ Show-Menu; $cmd = Read-Host "ENGREMIAT"; $cmd = ($cmd + "").Trim().ToLowerInvariant(); switch($cmd){ "" { continue } "1" { Invoke-ResolvedLauncher "Inicio operador" @("ENGREMIAT-LAUNCHER-ACTUAL.ps1","ENGREMIAT_OPERATOR.ps1","operator.ps1","launcher.ps1") @("ENGREMIAT-LAUNCHER-ACTUAL.ps1","launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1","launchers\ENGREMIAT-LAUNCHER-ACTUAL.ps1","tools\operator\ENGREMIAT-LAUNCHER-ACTUAL.ps1","tools\launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1","scripts\ENGREMIAT-LAUNCHER-ACTUAL.ps1") } "2" { Invoke-ResolvedLauncher "Proyectos" @("projects.ps1","ENGREMIAT-PROJECTS.ps1","project-launcher.ps1","ENGREMIAT-LAUNCHER-ACTUAL.ps1") @("launchers\projects.ps1","tools\operator\projects.ps1","operator\projects.ps1","ENGREMIAT-PROJECTS.ps1","ENGREMIAT-LAUNCHER-ACTUAL.ps1") } "3" { Invoke-ResolvedLauncher "Memoria y documentacion" @("memory-docs.ps1","ENGREMIAT-MEMORY-DOCS.ps1","docs.ps1","ENGREMIAT-LAUNCHER-ACTUAL.ps1") @("launchers\memory-docs.ps1","tools\operator\memory-docs.ps1","ENGREMIAT-MEMORY-DOCS.ps1","ENGREMIAT-LAUNCHER-ACTUAL.ps1") } "4" { Invoke-ResolvedLauncher "Data Intake" @("data-intake.ps1","ENGREMIAT-DATA-INTAKE.ps1","intake.ps1") @("launchers\data-intake.ps1","tools\operator\data-intake.ps1","ENGREMIAT-DATA-INTAKE.ps1") } "5" { Invoke-ResolvedLauncher "Health launchers" @("health-launchers.ps1","ENGREMIAT-HEALTH-LAUNCHERS.ps1","health.ps1") @("tools\health\health-launchers.ps1","launchers\health-launchers.ps1","ENGREMIAT-HEALTH-LAUNCHERS.ps1") } "m" { Invoke-ResolvedLauncher "Mantenimiento / tarjetas humanas" @("human-card-assistant.ps1") @("tools\human-card-assistant.ps1") } "?" { Show-Help } "h" { Show-Help } "help" { Show-Help } "b" { break } "q" { break } default { W "Comando no reconocido: $cmd" Yellow; $r=Wait-Back; if($r -eq "quit"){ break } } } } catch { if($_.Exception.Message -eq "__ENGREMIAT_QUIT__"){ break } else { throw } } }

## tools\human-card-assistant.ps1
- parse_ok: True
- has_common_legend: True
- legends:
  - while($true){ Clear-Host; W "==== ENGREMIAT / ASISTENTE DE TARJETAS HUMANAS ====" Cyan; W ""; W ("Almacen: "+$CardRoot) DarkGray; W "Contrato: humano -> tarjeta/tarea -> indice observable -> observer" DarkGray; W ""; W "[1] Crear tarjeta para revision del observer" White; W "[2] Crear tarea humana general" White; W "[3] Ver inbox de tarjetas" White; W "[4] Ver indice observable del observer" White; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "TARJETAS"; $cmd=($cmd+"").Trim().ToLowerInvariant(); switch($cmd){ "" { continue } "1" { New-HumanCard "OBSERVER_REVIEW" } "2" { New-HumanCard "HUMAN_TASK" } "3" { Show-Inbox } "4" { Show-Index } "?" { W "AYUDA" Cyan; W "Este asistente estandariza la comunicacion humano-Engremiat mediante tarjetas markdown e indice JSONL observable." White; W "El observer debe consumir documents/human-cards/_index/observer-inbox.jsonl." White; Read-Host "Enter = volver" | Out-Null } "b" { break } "q" { break } default { W "Comando no reconocido" Yellow; Read-Host "Enter = volver" | Out-Null } } }
- prompts:
  - function New-HumanCard([string]$Kind){ Ensure-Dirs; $now=Get-Date; $day=$now.ToString("yyyy\\MM\\dd"); $dir=Join-Path $Inbox $day; New-Item -ItemType Directory -Force -Path $dir | Out-Null; W ""; $title=Read-Host "Titulo breve de la tarjeta"; if([string]::IsNullOrWhiteSpace($title)){ $title="Revision humana" }; $summary=Read-Host "Resumen / decision necesaria"; if([string]::IsNullOrWhiteSpace($summary)){ $summary="Pendiente de completar por humano" }; $priority=Read-Host "Prioridad [LOW/MEDIUM/HIGH]"; $priority=($priority+"").Trim().ToUpperInvariant(); if($priority -notin @("LOW","MEDIUM","HIGH")){ $priority="MEDIUM" }; $id="HC-"+$now.ToString("yyyyMMdd-HHmmss")+"-"+(Get-Random -Minimum 1000 -Maximum 9999); $kindSafe=SafeText $Kind; $file=Join-Path $dir ($id+"-"+$kindSafe+".md"); $created=$now.ToString("s"); $body=@("# Tarjeta humana","","id: "+$id,"kind: "+$Kind,"status: PENDING_HUMAN_REVIEW","priority: "+$priority,"created_at: "+$created,"source: human-card-assistant","observer_access: documents/human-cards/_index/observer-inbox.jsonl","","## Titulo",$title,"","## Resumen / decision necesaria",$summary,"","## Checklist","- [ ] Revisar contexto","- [ ] Decidir siguiente accion","- [ ] Marcar decision","","## Decision","PENDING"); Set-Content -Path $file -Value $body -Encoding UTF8; $entry=[ordered]@{id=$id;kind=$Kind;status="PENDING_HUMAN_REVIEW";priority=$priority;created_at=$created;title=$title;summary=$summary;path=$file;observer_visible=$true}; $json=($entry|ConvertTo-Json -Compress); Add-Content -Path (Join-Path $Index "observer-inbox.jsonl") -Value $json -Encoding UTF8; Set-Content -Path (Join-Path $Index "latest-card.json") -Value $json -Encoding UTF8; W ""; W ("OK human_card_created="+$file) Green; W ("OK observer_index="+(Join-Path $Index "observer-inbox.jsonl")) Green; W "NEXT observer puede leer _index/observer-inbox.jsonl y abrir path." Cyan; Read-Host "Enter = volver" | Out-Null }
  - function Show-Inbox(){ Ensure-Dirs; W ""; W "INBOX TARJETAS HUMANAS" Cyan; $cards=Get-ChildItem $Inbox -Recurse -Filter *.md -ErrorAction SilentlyContinue | Sort-Object LastWriteTime -Descending | Select-Object -First 20; if(!$cards){ W "Sin tarjetas." Yellow } else { $i=1; foreach($c in $cards){ W ("["+$i+"] "+$c.FullName) White; $i++ } }; Read-Host "Enter = volver" | Out-Null }
  - function Show-Index(){ Ensure-Dirs; $idx=Join-Path $Index "observer-inbox.jsonl"; W ""; W "INDICE OBSERVER" Cyan; if(Test-Path $idx){ Get-Content $idx -Tail 20 | ForEach-Object { W $_ White } } else { W "Sin indice observer todavia." Yellow }; Read-Host "Enter = volver" | Out-Null }
  - while($true){ Clear-Host; W "==== ENGREMIAT / ASISTENTE DE TARJETAS HUMANAS ====" Cyan; W ""; W ("Almacen: "+$CardRoot) DarkGray; W "Contrato: humano -> tarjeta/tarea -> indice observable -> observer" DarkGray; W ""; W "[1] Crear tarjeta para revision del observer" White; W "[2] Crear tarea humana general" White; W "[3] Ver inbox de tarjetas" White; W "[4] Ver indice observable del observer" White; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "TARJETAS"; $cmd=($cmd+"").Trim().ToLowerInvariant(); switch($cmd){ "" { continue } "1" { New-HumanCard "OBSERVER_REVIEW" } "2" { New-HumanCard "HUMAN_TASK" } "3" { Show-Inbox } "4" { Show-Index } "?" { W "AYUDA" Cyan; W "Este asistente estandariza la comunicacion humano-Engremiat mediante tarjetas markdown e indice JSONL observable." White; W "El observer debe consumir documents/human-cards/_index/observer-inbox.jsonl." White; Read-Host "Enter = volver" | Out-Null } "b" { break } "q" { break } default { W "Comando no reconocido" Yellow; Read-Host "Enter = volver" | Out-Null } } }

## tools\data-intake.ps1
- parse_ok: True
- has_common_legend: True
- legends:
  - while($true){ Clear-Host; W "==== ENGREMIAT / DATA INTAKE ====" Cyan; W ""; W ("Almacen: "+$CardRoot) DarkGray; W "Contrato: entrada humana -> tarjeta DATA_INTAKE -> indice observable -> observer" DarkGray; W ""; W "[1] Crear entrada Data Intake" White; W "[2] Ver indice observable" White; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "DATA"; $cmd=($cmd+"").Trim().ToLowerInvariant(); switch($cmd){ "" { continue } "1" { New-DataIntakeCard } "2" { Show-Index } "m" { New-DataIntakeCard } "?" { W "AYUDA" Cyan; W "Data Intake convierte datos, ideas, necesidades o evidencias humanas en tarjetas observables para el observer." White; W "Indice: documents/human-cards/_index/observer-inbox.jsonl" White; Read-Host "Enter = volver" | Out-Null } "b" { break } "q" { break } default { W "Comando no reconocido" Yellow; Read-Host "Enter = volver" | Out-Null } } }
- prompts:
  - function New-DataIntakeCard(){ Ensure-Dirs; $now=Get-Date; $day=$now.ToString("yyyy\\MM\\dd"); $dir=Join-Path $Inbox $day; New-Item -ItemType Directory -Force -Path $dir | Out-Null; W ""; $title=Read-Host "Titulo / fuente de entrada"; if([string]::IsNullOrWhiteSpace($title)){ $title="Entrada humana" }; $summary=Read-Host "Dato, necesidad, observacion o idea"; if([string]::IsNullOrWhiteSpace($summary)){ $summary="Pendiente de completar" }; $id="DI-"+$now.ToString("yyyyMMdd-HHmmss")+"-"+(Get-Random -Minimum 1000 -Maximum 9999); $file=Join-Path $dir ($id+"-DATA_INTAKE.md"); $created=$now.ToString("s"); $body=@("# Data Intake","","id: "+$id,"kind: DATA_INTAKE","status: PENDING_CLASSIFICATION","priority: MEDIUM","created_at: "+$created,"source: tools/data-intake.ps1","observer_access: documents/human-cards/_index/observer-inbox.jsonl","","## Titulo / fuente",$title,"","## Entrada",$summary,"","## Clasificacion pendiente","- [ ] necesidad","- [ ] recurso","- [ ] bloqueo","- [ ] idea","- [ ] evidencia","","## Decision","PENDING"); Set-Content -Path $file -Value $body -Encoding UTF8; $entry=[ordered]@{id=$id;kind="DATA_INTAKE";status="PENDING_CLASSIFICATION";priority="MEDIUM";created_at=$created;title=$title;summary=$summary;path=$file;observer_visible=$true}; $json=($entry|ConvertTo-Json -Compress); Add-Content -Path (Join-Path $Index "observer-inbox.jsonl") -Value $json -Encoding UTF8; Set-Content -Path (Join-Path $Index "latest-data-intake.json") -Value $json -Encoding UTF8; W ""; W ("OK data_intake_card_created="+$file) Green; W ("OK observer_index="+(Join-Path $Index "observer-inbox.jsonl")) Green; W "NEXT observer clasifica esta entrada." Cyan; Read-Host "Enter = volver" | Out-Null }
  - function Show-Index(){ Ensure-Dirs; $idx=Join-Path $Index "observer-inbox.jsonl"; W ""; W "INDICE OBSERVABLE" Cyan; if(Test-Path $idx){ Get-Content $idx -Tail 20 | ForEach-Object { W $_ White } } else { W "Sin indice todavia." Yellow }; Read-Host "Enter = volver" | Out-Null }
  - while($true){ Clear-Host; W "==== ENGREMIAT / DATA INTAKE ====" Cyan; W ""; W ("Almacen: "+$CardRoot) DarkGray; W "Contrato: entrada humana -> tarjeta DATA_INTAKE -> indice observable -> observer" DarkGray; W ""; W "[1] Crear entrada Data Intake" White; W "[2] Ver indice observable" White; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "DATA"; $cmd=($cmd+"").Trim().ToLowerInvariant(); switch($cmd){ "" { continue } "1" { New-DataIntakeCard } "2" { Show-Index } "m" { New-DataIntakeCard } "?" { W "AYUDA" Cyan; W "Data Intake convierte datos, ideas, necesidades o evidencias humanas en tarjetas observables para el observer." White; W "Indice: documents/human-cards/_index/observer-inbox.jsonl" White; Read-Host "Enter = volver" | Out-Null } "b" { break } "q" { break } default { W "Comando no reconocido" Yellow; Read-Host "Enter = volver" | Out-Null } } }

## tools\health\health-launchers.ps1
- parse_ok: True
- has_common_legend: True
- legends:
  - function Run-Health(){ New-Item -ItemType Directory -Force -Path $ReportDir | Out-Null; Clear-Host; W "==== ENGREMIAT / HEALTH LAUNCHERS ====" Cyan; W ""; W ("Root: "+$Root) DarkGray; W "Contrato: revisar launchers reales enlazados desde ENGREMIAT.ps1" DarkGray; W ""; $items=@(); $items+=Test-LauncherPath "Launcher maestro" "ENGREMIAT.ps1"; $items+=Test-LauncherPath "Inicio operador" "tools\launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1"; $items+=Test-LauncherPath "Asistente tarjetas humanas" "tools\human-card-assistant.ps1"; $items+=Test-LauncherPath "Data Intake" "tools\data-intake.ps1"; $items+=Test-LauncherPath "Health launchers" "tools\health\health-launchers.ps1"; W ""; $ok=($items|Where-Object {$_.exists}).Count; $total=$items.Count; $missing=($items|Where-Object {-not $_.exists}).Count; $status=if($missing -eq 0){"PASS"}else{"WARN"}; W ("RESUMEN status="+$status+" ok="+$ok+" missing="+$missing+" total="+$total) $(if($missing -eq 0){"Green"}else{"Yellow"}); $report=Join-Path $ReportDir ("health-launchers-"+(Get-Date -Format "yyyyMMdd-HHmmss")+".json"); $payload=[ordered]@{package="ENGREMIAT_REAL_PROJECT_WALKTHROUGH_001";stage="E02J_CREATE_HEALTH_LAUNCHERS_MINIMAL_REAL";status=$status;created_at=(Get-Date -Format s);root=$Root;items=$items}; Set-Content -Path $report -Value ($payload|ConvertTo-Json -Depth 6) -Encoding UTF8; W ("OK report="+$report) Green; W "NEXT si falta algun launcher, crear pantalla minima real o enrutar al asistente de tarjetas." Cyan; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "HEALTH"; $cmd=($cmd+"").Trim().ToLowerInvariant(); if($cmd -eq "?" -or $cmd -eq "m"){ W ""; W "AYUDA" Cyan; W "Este health comprueba que las opciones del launcher maestro apunten a scripts reales." White; Read-Host "Enter = volver" | Out-Null } }
  - while($true){ Run-Health; $again=Read-Host "Enter = refrescar | b/q = volver"; $again=($again+"").Trim().ToLowerInvariant(); if($again -eq "b" -or $again -eq "q"){ break } }
- prompts:
  - function Run-Health(){ New-Item -ItemType Directory -Force -Path $ReportDir | Out-Null; Clear-Host; W "==== ENGREMIAT / HEALTH LAUNCHERS ====" Cyan; W ""; W ("Root: "+$Root) DarkGray; W "Contrato: revisar launchers reales enlazados desde ENGREMIAT.ps1" DarkGray; W ""; $items=@(); $items+=Test-LauncherPath "Launcher maestro" "ENGREMIAT.ps1"; $items+=Test-LauncherPath "Inicio operador" "tools\launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1"; $items+=Test-LauncherPath "Asistente tarjetas humanas" "tools\human-card-assistant.ps1"; $items+=Test-LauncherPath "Data Intake" "tools\data-intake.ps1"; $items+=Test-LauncherPath "Health launchers" "tools\health\health-launchers.ps1"; W ""; $ok=($items|Where-Object {$_.exists}).Count; $total=$items.Count; $missing=($items|Where-Object {-not $_.exists}).Count; $status=if($missing -eq 0){"PASS"}else{"WARN"}; W ("RESUMEN status="+$status+" ok="+$ok+" missing="+$missing+" total="+$total) $(if($missing -eq 0){"Green"}else{"Yellow"}); $report=Join-Path $ReportDir ("health-launchers-"+(Get-Date -Format "yyyyMMdd-HHmmss")+".json"); $payload=[ordered]@{package="ENGREMIAT_REAL_PROJECT_WALKTHROUGH_001";stage="E02J_CREATE_HEALTH_LAUNCHERS_MINIMAL_REAL";status=$status;created_at=(Get-Date -Format s);root=$Root;items=$items}; Set-Content -Path $report -Value ($payload|ConvertTo-Json -Depth 6) -Encoding UTF8; W ("OK report="+$report) Green; W "NEXT si falta algun launcher, crear pantalla minima real o enrutar al asistente de tarjetas." Cyan; W ""; W "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar" DarkGray; W ""; $cmd=Read-Host "HEALTH"; $cmd=($cmd+"").Trim().ToLowerInvariant(); if($cmd -eq "?" -or $cmd -eq "m"){ W ""; W "AYUDA" Cyan; W "Este health comprueba que las opciones del launcher maestro apunten a scripts reales." White; Read-Host "Enter = volver" | Out-Null } }
  - while($true){ Run-Health; $again=Read-Host "Enter = refrescar | b/q = volver"; $again=($again+"").Trim().ToLowerInvariant(); if($again -eq "b" -or $again -eq "q"){ break } }

## tools\launcher\ENGREMIAT-LAUNCHER-ACTUAL.ps1
- parse_ok: True
- has_common_legend: False
- legends:
  - Write-Host "Enter = refrescar pantalla" -ForegroundColor Gray
  - W "b/q = salir | Enter = refrescar" Cyan | ? = ayuda
- prompts:
  - Read-Host "Enter para volver" | Out-Null
  - function P(){ [void](Read-Host "Enter") }
  - $cmd=Read-Host "ENGREMIAT"


```

## Siguiente bloque recomendado
E03_VERIFY_ONE_SCREEN_MASTER_LAUNCHER_READONLY
