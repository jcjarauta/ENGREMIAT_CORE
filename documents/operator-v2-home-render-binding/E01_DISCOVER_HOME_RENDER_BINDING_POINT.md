# E01 Discover Home Render Binding Point

Estado: PASS
Patched: NO
Root patched: NO
ScreenEngine patched: NO
ScreenEngine: 
C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1
Overview: 
C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\operator-v2-live-context-overview.ps1
Template item exists: 
True
Renderer exists: 
True
Decision: 
GO_HOME_RENDER_CANDIDATE_FOUND_PREPARE_NO_APPLY_BIND

## Candidato principal
- function: Show-EngRootMenu
- line: 23
- end_line: 25
- score: 215
- why: title,menu,root-line,renders-output,template-structure,name-suggests-render

## Funciones candidatas
- score=215 | line=23-25 | Show-EngRootMenu | why=title,menu,root-line,renders-output,template-structure,name-suggests-render
- score=105 | line=19-19 | Show-EngLayoutRulesScreen | why=menu,renders-output,template-structure,name-suggests-render
- score=105 | line=18-18 | Show-EngScreenStatesScreen | why=menu,renders-output,template-structure,name-suggests-render
- score=105 | line=16-16 | Show-EngBody | why=menu,renders-output,template-structure,name-suggests-render
- score=105 | line=17-17 | Show-EngUxRegistryScreen | why=menu,renders-output,template-structure,name-suggests-render
- score=65 | line=21-21 | Show-EngProgressPendingScreen | why=renders-output,template-structure,name-suggests-render
- score=65 | line=20-20 | Show-EngPendingUxRepairsScreen | why=renders-output,template-structure,name-suggests-render
- score=65 | line=22-22 | Show-EngScreen | why=renders-output,template-structure,name-suggests-render
- score=65 | line=48-72 | Show-EngOperatorV2LiveContextOverviewPanel | why=renders-output,template-structure,name-suggests-render
- score=45 | line=8-8 | Get-EngScreen | why=template-structure,name-suggests-render
- score=45 | line=7-7 | Get-EngScreens | why=template-structure,name-suggests-render
- score=45 | line=11-11 | Show-EngFooter | why=renders-output,name-suggests-render
- score=45 | line=14-15 | Show-EngMaintenance | why=renders-output,name-suggests-render
- score=45 | line=13-13 | Show-EngHeader | why=renders-output,name-suggests-render
- score=20 | line=6-6 | Get-EngRegistry | why=template-structure
- score=20 | line=26-37 | Get-EngOperatorV2LiveContextOverviewTemplateItem | why=template-structure
- score=20 | line=10-10 | Get-EngChildren | why=template-structure

## Hits relevantes
- ENGREMIAT / OPERADOR V2 | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-EngOperator | line 48: function Show-EngOperatorV2LiveContextOverviewPanel {
- Show-Eng | line 11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
- Show-Eng | line 13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
- Show-Eng | line 14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
- Show-Eng | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- Show-Eng | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Show-Eng | line 48: function Show-EngOperatorV2LiveContextOverviewPanel {
- Screen | line 6: function Get-EngRegistry{Read-EngJson (Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json")}
- Screen | line 7: function Get-EngScreens{$r=Get-EngRegistry;if($r -and $r.screens){return @($r.screens)};return @()}
- Screen | line 8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
- Screen | line 10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
- Screen | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Screen | line 25: # LIVE_CONTEXT_SCREEN_010_RENDERER_BEGIN
- Screen | line 27: $style = 'C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_UX_STYLE.json'
- Screen | line 71: # LIVE_CONTEXT_SCREEN_010_RENDERER_END
- MENU | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- MENU | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Root: | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
- Write-Host | line 13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
- Write-Host | line 14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
- Write-Host | line 15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
- Write-Host | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- Write-Host | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 52: Write-Host "VISION GENERAL: contexto vivo no disponible" -ForegroundColor Yellow
- Write-Host | line 55: Write-Host ""
- Write-Host | line 56: Write-Host "VISION GENERAL / HOJA DE TAREAS" -ForegroundColor Cyan
- Write-Host | line 57: Write-Host (" Readiness plan: "+$data.readiness+"%") -ForegroundColor Green
- Write-Host | line 58: Write-Host (" Root estable: "+$data.root_stable+" | Wrapper LAB: "+$data.wrapper_lab+" | Feedback wrapper: "+$data.feedback_inside_wrapper) -ForegroundColor Green
- Write-Host | line 59: Write-Host (" Cerrados: "+$data.closed_count+" | Activos/LAB: "+$data.active_count+" | Pendientes: "+$data.pending_count+" | Total: "+$data.topics_total) -ForegroundColor Cyan
- Write-Host | line 60: Write-Host (" NEXT: "+$data.next_recommended) -ForegroundColor Magenta
- Write-Host | line 61: Write-Host ""
- Write-Host | line 62: Write-Host " Tareas principales:" -ForegroundColor Cyan
- Write-Host | line 66: Write-Host ("  ["+$t.status+"] "+$t.area+" · "+$t.readiness+"% · falta: "+$t.missing) -ForegroundColor $color
- Write-Host | line 68: Write-Host ""
- Write-Host | line 69: Write-Host " Acceso directo: plan / informe / estado-plan / 13" -ForegroundColor Cyan
- children | line 10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
- children | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- items | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- items | line 31: $items = @($styleObj.operator_v2_template_items)
- items | line 32: $item = @($items | Where-Object { $_.id -eq 'operator-v2-live-context-overview' } | Select-Object -First 1)
- screen | line 6: function Get-EngRegistry{Read-EngJson (Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json")}
- screen | line 7: function Get-EngScreens{$r=Get-EngRegistry;if($r -and $r.screens){return @($r.screens)};return @()}
- screen | line 8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
- screen | line 10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
- screen | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screen | line 25: # LIVE_CONTEXT_SCREEN_010_RENDERER_BEGIN
- screen | line 27: $style = 'C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_UX_STYLE.json'
- screen | line 71: # LIVE_CONTEXT_SCREEN_010_RENDERER_END
- template | line 26: function Get-EngOperatorV2LiveContextOverviewTemplateItem {
- template | line 31: $items = @($styleObj.operator_v2_template_items)
- template | line 49: $item = Get-EngOperatorV2LiveContextOverviewTemplateItem
- Render | line 25: # LIVE_CONTEXT_SCREEN_010_RENDERER_BEGIN
- Render | line 71: # LIVE_CONTEXT_SCREEN_010_RENDERER_END
- LIVE_CONTEXT_SCREEN_010_RENDERER_BEGIN | line 25: # LIVE_CONTEXT_SCREEN_010_RENDERER_BEGIN
- Show-EngOperatorV2LiveContextOverviewPanel | line 48: function Show-EngOperatorV2LiveContextOverviewPanel {

## Recomendacion
No aplicar bind automatico hasta revisar el candidato principal. El renderer disponible es Show-EngOperatorV2LiveContextOverviewPanel.

## Siguiente
E02_PREPARE_HOME_RENDER_BINDING_PLAN_NO_APPLY
