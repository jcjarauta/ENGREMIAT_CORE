# E03 R2 Screen Engine Template Diagnosis

Estado: PASS
Patched screen: NO
Screen engine: 
C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1
Alias plan-v2: 
C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\plan-v2.ps1
Decision: 
GO_PATCH_TEMPLATE_OR_CONTRACT_NEXT_NOT_SCREEN_TEXT

## Lectura
El fallo anterior indica que el menu visible no esta como texto plano numerado dentro del ScreenEngine. Probablemente se renderiza desde estructura/contrato/plantilla.

## Funciones detectadas
- line 5: Read-EngJson
- line 6: Get-EngRegistry
- line 7: Get-EngScreens
- line 8: Get-EngScreen
- line 9: Get-EngLevelSortKey
- line 10: Get-EngChildren
- line 11: Show-EngFooter
- line 12: Read-EngCmd
- line 13: Show-EngHeader
- line 14: Show-EngMaintenance
- line 16: Show-EngBody
- line 17: Show-EngUxRegistryScreen
- line 18: Show-EngScreenStatesScreen
- line 19: Show-EngLayoutRulesScreen
- line 20: Show-EngPendingUxRepairsScreen
- line 21: Show-EngProgressPendingScreen
- line 22: Show-EngScreen
- line 23: Show-EngRootMenu

## Hits relevantes
- ENGREMIAT / OPERADOR V2 | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- ENGREMIAT_V2 | line 4: $script:EngPrompt="ENGREMIAT_V2"
- MENU | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- MENU | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- MENU | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 7: function Get-EngScreens{$r=Get-EngRegistry;if($r -and $r.screens){return @($r.screens)};return @()}
- screens | line 8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
- screens | line 10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
- screens | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- screens | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- items | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- children | line 10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
- children | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- route | line 13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
- Read-Host | line 12: function Read-EngCmd{$x=Read-Host $script:EngPrompt;if([string]::IsNullOrWhiteSpace($x)){return "refresh"};return $x.Trim().ToLowerInvariant()}
- Read-Host | line 14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
- Read-Host | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- switch | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
- Write-Host | line 13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
- Write-Host | line 14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
- Write-Host | line 15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
- Write-Host | line 16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
- Write-Host | line 17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
- Write-Host | line 23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}

## Contratos candidatos como fuente de menu
- score=155 | why=title,menu-structure,prompt | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_UX_STYLE.json
- score=125 | why=dev-system,reports,menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_REGISTRY.before-e05b-20260621-175913.json
- score=125 | why=dev-system,reports,menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_REGISTRY.before-e04b-20260621-175346.json
- score=125 | why=dev-system,reports,menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_REGISTRY.before-e03-20260621-175039.json
- score=125 | why=dev-system,reports,menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_REGISTRY.json
- score=75 | why=menu-structure,prompt | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\OPERATOR_V2_UI_CONTRACT.json
- score=25 | why=menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\COMMAND_FEEDBACK.json
- score=25 | why=menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\ROOT_LOOP_ROUTER.json
- score=25 | why=menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\NAVIGATION_COMMANDS.json
- score=25 | why=menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_ARCHITECTURE_CONTRACT.json
- score=25 | why=menu-structure | C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\ROOT_WRAPPER.json

## JSON refs en ScreenEngine
- contracts\SCREEN_REGISTRY.json

## Comando ya disponible
```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\plan-v2.ps1"
```

## Siguiente
E04_PATCH_REAL_MENU_SOURCE_WITH_PLAN_REPORT_ENTRY_GUARDED_WRITE
