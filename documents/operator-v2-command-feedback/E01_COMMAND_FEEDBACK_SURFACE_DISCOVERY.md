# E01 Command Feedback Surface Discovery

Estado: PASS
Launcher: 
C:\ENGREMIAT_CORE\ENGREMIAT.ps1
Operator root: 
C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2
Patched: NO
Readiness: 
100
Actions observer: 
True
Navigation observer: 
True
Hits: 
32

## Objetivo COMMAND_FEEDBACK_006
- Enter vacio = refrescar/redibujar.
- ? = ayuda contextual.
- m = mantenimiento o PENDIENTE claro.
- e = estado compacto o PENDIENTE claro.
- desconocido = Comando no reconocido. Usa ? para ayuda.
- b/q = volver/salir consistente.

## Top hits
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 22 score 45 why read_host,switch,feedback_text,nav_tokens,fallback parse_ok True
```powershell
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 14 score 31 why read_host,feedback_text,nav_tokens parse_ok True
```powershell
   10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
   11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
   12: function Read-EngCmd{$x=Read-Host $script:EngPrompt;if([string]::IsNullOrWhiteSpace($x)){return "refresh"};return $x.Trim().ToLowerInvariant()}
   13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 20 score 25 why switch,feedback_text,nav_tokens,fallback parse_ok True
```powershell
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 21 score 25 why switch,feedback_text,nav_tokens,fallback parse_ok True
```powershell
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 11 score 25 why read_host,nav_tokens parse_ok True
```powershell
    7: function Show-EngHeader { param([string]$Title,[string]$Route,[string]$Purpose) Clear-Host; Write-Host ("==== ENGREMIAT / " + $Title + " ====") -ForegroundColor Cyan; Write-Host ""; Write-Host "Modo: paralelo / no rompe operador actual"; Write-Host ("Core: " + $script:EngCoreRoot); if($Route){ Write-Host ("Ruta: " + $Route) }; if($Purpose){ Write-Host ("Proposito: " + $Purpose) }; $s=Get-EngState; Write-Host ""; Write-Host ("Observer: " + $s.observer_status + " | needs=" + $s.observer_needs); Write-Host ("Watch: " + $s.watch_status + " | changes=" + $s.watch_changes + " | tracked=" + $s.watch_tracked); Write-Host "" }
    8: function Show-EngFooter { Write-Host ""; Write-Host $script:EngFooter -ForegroundColor Gray; Write-Host "" }
    9: function Show-EngMenu { param([array]$Items) Write-Host "MENU"; foreach($it in $Items){ Write-Host (" [" + $it.key + "] " + $it.label) }; Show-EngFooter }
   10: function Read-EngCommand { return (Read-Host $script:EngPrompt) }
   11: function Pause-Eng { param([string]$Message="Enter para volver") Write-Host ""; Read-Host $Message | Out-Null }
   12: 
   13: function Save-EngOperatorState {
   14:   $out = Join-Path $script:EngV2Root "memory\OPERATOR_STATE.json"
   15:   New-Item -ItemType Directory -Force -Path (Split-Path $out -Parent) | Out-Null
   16:   $state = Build-EngOperatorState
   17:   $state | ConvertTo-Json -Depth 14 | Set-Content -LiteralPath $out -Encoding UTF8
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 12 score 20 why read_host parse_ok True
```powershell
    8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
    9: function Get-EngLevelSortKey($s){$parts=(([string]$s.level).Split("."));$nums=@();foreach($p in $parts){$n=0;[void][int]::TryParse(($p -replace "[^0-9]",""),[ref]$n);$nums+=("{0:D4}" -f $n)};return ($nums -join ".")}
   10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
   11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
   12: function Read-EngCmd{$x=Read-Host $script:EngPrompt;if([string]::IsNullOrWhiteSpace($x)){return "refresh"};return $x.Trim().ToLowerInvariant()}
   13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 10 score 20 why read_host parse_ok True
```powershell
    6: function Get-EngState { $obs=Read-EngJson (Join-Path $script:EngCoreRoot "reports\observer\latest-observer.json"); $watch=Read-EngJson (Join-Path $script:EngCoreRoot "memory\development\watch-state.json"); return [ordered]@{ observer_status = if($obs){[string]$obs.status}else{"NO_LATEST"}; observer_needs = if($obs){@($obs.needs).Count}else{-1}; watch_status = if($watch){[string]$watch.status}else{"NO_STATE"}; watch_changes = if($watch){[int]$watch.changes_count}else{-1}; watch_tracked = if($watch){[int]$watch.tracked_files}else{-1}; core_root = $script:EngCoreRoot; v2_root = $script:EngV2Root } }
    7: function Show-EngHeader { param([string]$Title,[string]$Route,[string]$Purpose) Clear-Host; Write-Host ("==== ENGREMIAT / " + $Title + " ====") -ForegroundColor Cyan; Write-Host ""; Write-Host "Modo: paralelo / no rompe operador actual"; Write-Host ("Core: " + $script:EngCoreRoot); if($Route){ Write-Host ("Ruta: " + $Route) }; if($Purpose){ Write-Host ("Proposito: " + $Purpose) }; $s=Get-EngState; Write-Host ""; Write-Host ("Observer: " + $s.observer_status + " | needs=" + $s.observer_needs); Write-Host ("Watch: " + $s.watch_status + " | changes=" + $s.watch_changes + " | tracked=" + $s.watch_tracked); Write-Host "" }
    8: function Show-EngFooter { Write-Host ""; Write-Host $script:EngFooter -ForegroundColor Gray; Write-Host "" }
    9: function Show-EngMenu { param([array]$Items) Write-Host "MENU"; foreach($it in $Items){ Write-Host (" [" + $it.key + "] " + $it.label) }; Show-EngFooter }
   10: function Read-EngCommand { return (Read-Host $script:EngPrompt) }
   11: function Pause-Eng { param([string]$Message="Enter para volver") Write-Host ""; Read-Host $Message | Out-Null }
   12: 
   13: function Save-EngOperatorState {
   14:   $out = Join-Path $script:EngV2Root "memory\OPERATOR_STATE.json"
   15:   New-Item -ItemType Directory -Force -Path (Split-Path $out -Parent) | Out-Null
   16:   $state = Build-EngOperatorState
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 127 score 20 why read_host parse_ok True
```powershell
  123: function Save-EngOperatorStateFast { $p=Join-Path $script:EngV2Root 'memory\OPERATOR_STATE.json'; New-Item -ItemType Directory -Force -Path (Split-Path $p -Parent)|Out-Null; $s=Build-EngOperatorStateFast; $s|ConvertTo-Json -Depth 8|Set-Content -LiteralPath $p -Encoding UTF8; return $s }
  124: function Show-EngStateScreenFast { $s=Save-EngOperatorStateFast; Show-EngHeader 'ESTADO DEL SISTEMA' 'state' 'Resumen rapido de CORE y Operador V2'; Write-Host ('Decision: '+$s.decision); Write-Host ''; Write-Host ('Observer: '+$s.observer_status+' | needs='+$s.observer_needs+' | bloqueantes='+$s.blocking_needs); Write-Host ('Watch: '+$s.watch_status+' | cambios='+$s.watch_changes+' | tracked='+$s.watch_tracked); Write-Host ''; Write-Host 'Rutas:'; Write-Host (' - OPERATOR_STATE: '+$s.operator_state); Write-Host (' - Observer: '+$s.latest_observer); Write-Host (' - Watch: '+$s.watch_state); Write-Host (' - Timeline: '+$s.timeline); Write-Host ''; Write-Host ('Siguiente: '+$s.next); Show-EngFooter; Pause-Eng }
  125: 
  126: 
  127: function Read-EngScreenCommand { $x=Read-Host $script:EngPrompt; if([string]::IsNullOrWhiteSpace($x)){return 'refresh'}; return $x.ToLowerInvariant() }
  128: function Invoke-EngScreenLoop { param([scriptblock]$Render,[scriptblock]$Maintenance) while($true){ & $Render; $cmd=Read-EngScreenCommand; switch($cmd){ 'refresh'{continue} 'b'{return} 'q'{return} '?' {Show-EngHelp;continue} 'm'{ if($Maintenance){& $Maintenance}else{Show-EngMaintenanceV2}; continue } default { Write-Host 'Opcion no reconocida' -ForegroundColor Yellow; Pause-Eng; continue } } } }
  129: 
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 17 score 19 why switch,nav_tokens,fallback parse_ok True
```powershell
   13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 18 score 19 why switch,nav_tokens,fallback parse_ok True
```powershell
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 19 score 19 why switch,nav_tokens,fallback parse_ok True
```powershell
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 23 score 19 why switch,nav_tokens,fallback parse_ok True
```powershell
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function Show-EngRootMenu{while($true){$roots=@(Get-EngScreens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$map=@{};Clear-Host;Write-Host "==== ENGREMIAT / OPERADOR V2 ====" -ForegroundColor Cyan;Write-Host "";Write-Host ("Root: "+$script:EngV2Root);Write-Host "";Write-Host "MENU";$i=1;foreach($s in $roots){Write-Host (" ["+$i+"] "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]");$map[[string]$i]=$s.id;$i++};Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{break}"q"{break}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Selecciona una pantalla principal. Enter refresca.";Start-Sleep -Seconds 1;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 128 score 19 why switch,nav_tokens,fallback parse_ok True
```powershell
  124: function Show-EngStateScreenFast { $s=Save-EngOperatorStateFast; Show-EngHeader 'ESTADO DEL SISTEMA' 'state' 'Resumen rapido de CORE y Operador V2'; Write-Host ('Decision: '+$s.decision); Write-Host ''; Write-Host ('Observer: '+$s.observer_status+' | needs='+$s.observer_needs+' | bloqueantes='+$s.blocking_needs); Write-Host ('Watch: '+$s.watch_status+' | cambios='+$s.watch_changes+' | tracked='+$s.watch_tracked); Write-Host ''; Write-Host 'Rutas:'; Write-Host (' - OPERATOR_STATE: '+$s.operator_state); Write-Host (' - Observer: '+$s.latest_observer); Write-Host (' - Watch: '+$s.watch_state); Write-Host (' - Timeline: '+$s.timeline); Write-Host ''; Write-Host ('Siguiente: '+$s.next); Show-EngFooter; Pause-Eng }
  125: 
  126: 
  127: function Read-EngScreenCommand { $x=Read-Host $script:EngPrompt; if([string]::IsNullOrWhiteSpace($x)){return 'refresh'}; return $x.ToLowerInvariant() }
  128: function Invoke-EngScreenLoop { param([scriptblock]$Render,[scriptblock]$Maintenance) while($true){ & $Render; $cmd=Read-EngScreenCommand; switch($cmd){ 'refresh'{continue} 'b'{return} 'q'{return} '?' {Show-EngHelp;continue} 'm'{ if($Maintenance){& $Maintenance}else{Show-EngMaintenanceV2}; continue } default { Write-Host 'Opcion no reconocida' -ForegroundColor Yellow; Pause-Eng; continue } } } }
  129: 
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-ux-style-reader.ps1 line 27 score 8 why prompt parse_ok True
```powershell
   23:   param([object]$Style,[string]$Text)
   24:   if($null -eq $Style){ throw 'Style requerido' }
   25:   if($null -eq $Text){ $Text = '' }
   26:   $legend = [string]$Style.legend.canonical
   27:   return [pscustomobject]@{ has_canonical_legend=$Text.Contains($legend); has_title=($Text.Contains('ENGREMIAT /')); has_menu=($Text.Contains('MENU') -or $Text.Contains('SUBMENU')); has_prompt=($Text.Contains('ENGREMIAT:') -or $Text.Contains('ENGREMIAT_V2:')); text_length=$Text.Length }
   28: }
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 4 score 8 why prompt parse_ok True
```powershell
    1: $script:EngV2Root="C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2"
    2: $script:EngCoreRoot="C:\ENGREMIAT_CORE"
    3: "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar"
    4: $script:EngPrompt="ENGREMIAT_V2"
    5: function Read-EngJson([string]$Path){try{if(Test-Path -LiteralPath $Path){return Get-Content -LiteralPath $Path -Raw -Encoding UTF8|ConvertFrom-Json}}catch{};return $null}
    6: function Get-EngRegistry{Read-EngJson (Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json")}
    7: function Get-EngScreens{$r=Get-EngRegistry;if($r -and $r.screens){return @($r.screens)};return @()}
    8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
    9: function Get-EngLevelSortKey($s){$parts=(([string]$s.level).Split("."));$nums=@();foreach($p in $parts){$n=0;[void][int]::TryParse(($p -replace "[^0-9]",""),[ref]$n);$nums+=("{0:D4}" -f $n)};return ($nums -join ".")}
   10: function Get-EngChildren($Screen){$all=Get-EngScreens;$out=@();foreach($cid in @($Screen.children)){$c=@($all|Where-Object{[string]$_.id -eq [string]$cid}|Select-Object -First 1);if($c){$out+=$c}};return @($out)}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 4 score 8 why prompt parse_ok True
```powershell
    1: $script:EngV2Root = "C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2"
    2: $script:EngCoreRoot = "C:\ENGREMIAT_CORE"
    3: $script:EngFooter = "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar"
    4: $script:EngPrompt = "ENGREMIAT_V2"
    5: function Read-EngJson { param([string]$Path) try { if(Test-Path -LiteralPath $Path){ return Get-Content -LiteralPath $Path -Raw | ConvertFrom-Json } } catch {} return $null }
    6: function Get-EngState { $obs=Read-EngJson (Join-Path $script:EngCoreRoot "reports\observer\latest-observer.json"); $watch=Read-EngJson (Join-Path $script:EngCoreRoot "memory\development\watch-state.json"); return [ordered]@{ observer_status = if($obs){[string]$obs.status}else{"NO_LATEST"}; observer_needs = if($obs){@($obs.needs).Count}else{-1}; watch_status = if($watch){[string]$watch.status}else{"NO_STATE"}; watch_changes = if($watch){[int]$watch.changes_count}else{-1}; watch_tracked = if($watch){[int]$watch.tracked_files}else{-1}; core_root = $script:EngCoreRoot; v2_root = $script:EngV2Root } }
    7: function Show-EngHeader { param([string]$Title,[string]$Route,[string]$Purpose) Clear-Host; Write-Host ("==== ENGREMIAT / " + $Title + " ====") -ForegroundColor Cyan; Write-Host ""; Write-Host "Modo: paralelo / no rompe operador actual"; Write-Host ("Core: " + $script:EngCoreRoot); if($Route){ Write-Host ("Ruta: " + $Route) }; if($Purpose){ Write-Host ("Proposito: " + $Purpose) }; $s=Get-EngState; Write-Host ""; Write-Host ("Observer: " + $s.observer_status + " | needs=" + $s.observer_needs); Write-Host ("Watch: " + $s.watch_status + " | changes=" + $s.watch_changes + " | tracked=" + $s.watch_tracked); Write-Host "" }
    8: function Show-EngFooter { Write-Host ""; Write-Host $script:EngFooter -ForegroundColor Gray; Write-Host "" }
    9: function Show-EngMenu { param([array]$Items) Write-Host "MENU"; foreach($it in $Items){ Write-Host (" [" + $it.key + "] " + $it.label) }; Show-EngFooter }
   10: function Read-EngCommand { return (Read-Host $script:EngPrompt) }
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 16 score 6 why feedback_text parse_ok True
```powershell
   12: function Read-EngCmd{$x=Read-Host $script:EngPrompt;if([string]::IsNullOrWhiteSpace($x)){return "refresh"};return $x.Trim().ToLowerInvariant()}
   13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   22: function Show-EngScreen([string]$Id){if($Id -eq "progreso_pendientes"){Show-EngProgressPendingScreen;return};if($Id -eq "ux_registry"){Show-EngUxRegistryScreen;return};if($Id -eq "screen_states"){Show-EngScreenStatesScreen;return};if($Id -eq "layout_rules"){Show-EngLayoutRulesScreen;return};if($Id -eq "pending_ux_repairs"){Show-EngPendingUxRepairsScreen;return};$s=Get-EngScreen $Id;if(!$s){Clear-Host;Write-Host ("Pantalla no encontrada: "+$Id) -ForegroundColor Yellow;Read-Host "Enter para volver"|Out-Null;return};while($true){Show-EngHeader $s.title ($s.level+" · "+$s.id) $s.status ([int]$s.progress) $s.purpose;$map=Show-EngBody $s;Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Show-EngHelp $s;continue}default{if($map.ContainsKey($cmd)){Show-EngScreen $map[$cmd];continue};Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 49 score 6 why feedback_text parse_ok True
```powershell
   45: }
   46: 
   47: function Show-EngMaintenanceV2 {
   48:   Show-EngHeader "MANTENIMIENTO V2" "maintenance" "Herramientas locales del operador paralelo"
   49:   Write-Host "Comandos disponibles:"
   50:   Write-Host " - status: pantalla principal del operador"
   51:   Write-Host " - observer: usar obs.ps1 desde CORE si hace falta"
   52:   Write-Host " - timeline: abrir memoria de desarrollo"
   53:   Write-Host ""
   54:   Write-Host "Rutas V2:"
   55:   Write-Host (" - Root: " + $script:EngV2Root)
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\navigation-launcher-bridge.ps1 line 17 score 5 why nav_tokens parse_ok True
```powershell
   13:   if($dry.ok -eq $true){ $handled = $true }
   14:   return [pscustomobject]@{ ok=$dry.ok; handled=$handled; input=$InputText; normalized=$dry.normalized; command_id=$dry.command_id; behavior=$dry.behavior; mode='DRY_RUN_ONLY'; legacy_fallback=$dry.legacy_fallback; executed=$false; real_execution=$false; decision='NAVIGATION_LAUNCHER_BRIDGE_READY_LEGACY_FLOW_PRESERVED' }
   15: }
   16: function Test-EngNavigationLauncherBridgeInputs {
   17:   $inputs = @('','enter','refrescar','f5','?','b','q','m','e','__unknown__')
   18:   $results = @()
   19:   foreach($inp in $inputs){ $results += Invoke-EngNavigationLauncherInputDryRun -InputText $inp }
   20:   return $results
   21: }
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-router-adapter.ps1 line 7 score 5 why nav_tokens parse_ok True
```powershell
    3: if(!(Test-Path $script:EngScreenActionsEnginePath)){ throw "Engine no encontrado: $script:EngScreenActionsEnginePath" }
    4: . $script:EngScreenActionsEnginePath
    5: function Get-EngScreenActionRouterAdapterState {
    6:   $state = Get-EngScreenActionsEngineState
    7:   return [pscustomobject]@{ status='READY'; mode='DRY_RUN_ONLY'; action_count=$state.action_count; action_ids=@($state.action_ids); supported_inputs=@('e','m','?','q','b','r','f5','enter') }
    8: }
    9: function Resolve-EngScreenActionInput {
   10:   param([string]$InputText)
   11:   $state = Get-EngScreenActionsEngineState
   12:   $inputNorm = if($null -eq $InputText){''}else{([string]$InputText).Trim().ToLowerInvariant()}
   13:   $ids = @($state.action_ids)
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-router-adapter.ps1 line 14 score 5 why nav_tokens parse_ok True
```powershell
   10:   param([string]$InputText)
   11:   $state = Get-EngScreenActionsEngineState
   12:   $inputNorm = if($null -eq $InputText){''}else{([string]$InputText).Trim().ToLowerInvariant()}
   13:   $ids = @($state.action_ids)
   14:   if($inputNorm -eq '' -or $inputNorm -eq 'enter' -or $inputNorm -eq 'f5' -or $inputNorm -eq 'refrescar'){ $hit = @($ids | Where-Object { $_ -match 'refresh|redraw|home|inicio|registry|estado' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   15:   if($inputNorm -eq 'e' -or $inputNorm -eq 'estado'){ $hit = @($ids | Where-Object { $_ -match '\.e$|estado|status|registry' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   16:   if($inputNorm -eq 'm' -or $inputNorm -eq 'menu'){ $hit = @($ids | Where-Object { $_ -match 'menu|maintenance|mantenimiento' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   17:   if($inputNorm -eq '?' -or $inputNorm -eq 'ayuda' -or $inputNorm -eq 'help'){ $hit = @($ids | Where-Object { $_ -match 'help|ayuda|\?' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   18:   if($inputNorm -eq 'q' -or $inputNorm -eq 'b' -or $inputNorm -eq 'salir' -or $inputNorm -eq 'volver'){ $hit = @($ids | Where-Object { $_ -match 'back|volver|exit|salir|quit' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   19:   if($ids -contains $InputText){ return $InputText }
   20:   return $null
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-router-adapter.ps1 line 16 score 5 why nav_tokens parse_ok True
```powershell
   12:   $inputNorm = if($null -eq $InputText){''}else{([string]$InputText).Trim().ToLowerInvariant()}
   13:   $ids = @($state.action_ids)
   14:   if($inputNorm -eq '' -or $inputNorm -eq 'enter' -or $inputNorm -eq 'f5' -or $inputNorm -eq 'refrescar'){ $hit = @($ids | Where-Object { $_ -match 'refresh|redraw|home|inicio|registry|estado' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   15:   if($inputNorm -eq 'e' -or $inputNorm -eq 'estado'){ $hit = @($ids | Where-Object { $_ -match '\.e$|estado|status|registry' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   16:   if($inputNorm -eq 'm' -or $inputNorm -eq 'menu'){ $hit = @($ids | Where-Object { $_ -match 'menu|maintenance|mantenimiento' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   17:   if($inputNorm -eq '?' -or $inputNorm -eq 'ayuda' -or $inputNorm -eq 'help'){ $hit = @($ids | Where-Object { $_ -match 'help|ayuda|\?' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   18:   if($inputNorm -eq 'q' -or $inputNorm -eq 'b' -or $inputNorm -eq 'salir' -or $inputNorm -eq 'volver'){ $hit = @($ids | Where-Object { $_ -match 'back|volver|exit|salir|quit' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   19:   if($ids -contains $InputText){ return $InputText }
   20:   return $null
   21: }
   22: function Invoke-EngScreenRouterInputDryRun {
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-router-adapter.ps1 line 17 score 5 why nav_tokens parse_ok True
```powershell
   13:   $ids = @($state.action_ids)
   14:   if($inputNorm -eq '' -or $inputNorm -eq 'enter' -or $inputNorm -eq 'f5' -or $inputNorm -eq 'refrescar'){ $hit = @($ids | Where-Object { $_ -match 'refresh|redraw|home|inicio|registry|estado' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   15:   if($inputNorm -eq 'e' -or $inputNorm -eq 'estado'){ $hit = @($ids | Where-Object { $_ -match '\.e$|estado|status|registry' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   16:   if($inputNorm -eq 'm' -or $inputNorm -eq 'menu'){ $hit = @($ids | Where-Object { $_ -match 'menu|maintenance|mantenimiento' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   17:   if($inputNorm -eq '?' -or $inputNorm -eq 'ayuda' -or $inputNorm -eq 'help'){ $hit = @($ids | Where-Object { $_ -match 'help|ayuda|\?' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   18:   if($inputNorm -eq 'q' -or $inputNorm -eq 'b' -or $inputNorm -eq 'salir' -or $inputNorm -eq 'volver'){ $hit = @($ids | Where-Object { $_ -match 'back|volver|exit|salir|quit' } | Select-Object -First 1); if($hit.Count -gt 0){ return [string]$hit[0] } }
   19:   if($ids -contains $InputText){ return $InputText }
   20:   return $null
   21: }
   22: function Invoke-EngScreenRouterInputDryRun {
   23:   param([string]$InputText)
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-ux-style-reader.ps1 line 11 score 5 why nav_tokens parse_ok True
```powershell
    7:   if([string]::IsNullOrWhiteSpace($raw)){ throw "Contrato vacio: $Path" }
    8:   $json = $raw | ConvertFrom-Json
    9:   if($json.schema -ne 'ENGREMIAT_SCREEN_UX_STYLE_V1'){ throw 'Schema invalido' }
   10:   if([string]::IsNullOrWhiteSpace([string]$json.legend.canonical)){ throw 'legend.canonical ausente' }
   11:   if(-not ([string]$json.legend.canonical).Contains('Enter = refrescar')){ throw 'legend.canonical sin Enter = refrescar' }
   12:   if(-not ([string]$json.legend.canonical).Contains('m = mantenimiento')){ throw 'legend.canonical sin m = mantenimiento' }
   13:   $sections = @($json.sections)
   14:   if($sections.Count -lt 5){ throw "sections insuficientes: $($sections.Count)" }
   15:   return [pscustomobject]@{ path=$Path; schema=$json.schema; policy=$json.policy; layout=$json.layout; legend=$json.legend; sections=$sections; command_feedback=$json.command_feedback; section_count=$sections.Count }
   16: }
   17: function Get-EngScreenUxStyleSummary {
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-ux-style-reader.ps1 line 12 score 5 why nav_tokens parse_ok True
```powershell
    8:   $json = $raw | ConvertFrom-Json
    9:   if($json.schema -ne 'ENGREMIAT_SCREEN_UX_STYLE_V1'){ throw 'Schema invalido' }
   10:   if([string]::IsNullOrWhiteSpace([string]$json.legend.canonical)){ throw 'legend.canonical ausente' }
   11:   if(-not ([string]$json.legend.canonical).Contains('Enter = refrescar')){ throw 'legend.canonical sin Enter = refrescar' }
   12:   if(-not ([string]$json.legend.canonical).Contains('m = mantenimiento')){ throw 'legend.canonical sin m = mantenimiento' }
   13:   $sections = @($json.sections)
   14:   if($sections.Count -lt 5){ throw "sections insuficientes: $($sections.Count)" }
   15:   return [pscustomobject]@{ path=$Path; schema=$json.schema; policy=$json.policy; layout=$json.layout; legend=$json.legend; sections=$sections; command_feedback=$json.command_feedback; section_count=$sections.Count }
   16: }
   17: function Get-EngScreenUxStyleSummary {
   18:   param([object]$Style)
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 3 score 5 why nav_tokens parse_ok True
```powershell
    1: $script:EngV2Root="C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2"
    2: $script:EngCoreRoot="C:\ENGREMIAT_CORE"
    3: "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar"
    4: $script:EngPrompt="ENGREMIAT_V2"
    5: function Read-EngJson([string]$Path){try{if(Test-Path -LiteralPath $Path){return Get-Content -LiteralPath $Path -Raw -Encoding UTF8|ConvertFrom-Json}}catch{};return $null}
    6: function Get-EngRegistry{Read-EngJson (Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json")}
    7: function Get-EngScreens{$r=Get-EngRegistry;if($r -and $r.screens){return @($r.screens)};return @()}
    8: function Get-EngScreen([string]$Id){return @(Get-EngScreens|Where-Object{[string]$_.id -eq $Id}|Select-Object -First 1)}
    9: function Get-EngLevelSortKey($s){$parts=(([string]$s.level).Split("."));$nums=@();foreach($p in $parts){$n=0;[void][int]::TryParse(($p -replace "[^0-9]",""),[ref]$n);$nums+=("{0:D4}" -f $n)};return ($nums -join ".")}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.ScreenEngine.ps1 line 15 score 5 why nav_tokens parse_ok True
```powershell
   11: function Show-EngFooter{Write-Host "";Write-Host $script:EngFooter -ForegroundColor Gray;Write-Host ""}
   12: function Read-EngCmd{$x=Read-Host $script:EngPrompt;if([string]::IsNullOrWhiteSpace($x)){return "refresh"};return $x.Trim().ToLowerInvariant()}
   13: function Show-EngHeader([string]$Title,[string]$Route,[string]$Status,[int]$Progress,[string]$Purpose){Clear-Host;Write-Host ("==== ENGREMIAT / "+$Title+" ====") -ForegroundColor Cyan;Write-Host "";if($Route){Write-Host ("Ruta: "+$Route) -ForegroundColor DarkCyan};if($Status){Write-Host ("Estado: "+$Status+" · "+$Progress+"%") -ForegroundColor DarkCyan};if($Purpose){Write-Host ("Objetivo: "+$Purpose)};Write-Host ""}
   14: function Show-EngMaintenance{Show-EngHeader "MANTENIMIENTO V2" "mantenimiento" "PLANNED" 0 "Mantenimiento contextual pendiente";Write-Host "Pendiente: conectar tarjetas de mantenimiento.";Show-EngFooter;Read-Host "Enter para volver"|Out-Null}
   15: Write-Host "[b/q] volver | m = mantenimiento | ? = ayuda | Enter = refrescar" -ForegroundColor DarkGray
   16: function Show-EngBody($s){$children=Get-EngChildren $s;$map=@{};if($children.Count -gt 0){Write-Host "MENU";$i=1;foreach($c in ($children|Sort-Object {Get-EngLevelSortKey $_})){Write-Host (" ["+$i+"] "+$c.title+"  ["+$c.status+" · "+$c.progress+"%]");$map[[string]$i]=$c.id;$i++};Write-Host ""};$p=@($s.pending);if($p.Count -gt 0){Write-Host "PENDIENTE PRINCIPAL:";Write-Host (" - "+$p[0]);if($p.Count -gt 1){Write-Host (" - +"+($p.Count-1)+" pendientes mas en Reparaciones UX")};Write-Host ""};return $map}
   17: function Show-EngUxRegistryScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;$roots=@($screens|Where-Object{[string]$_.level -notmatch "\."}|Sort-Object {Get-EngLevelSortKey $_});$active=@($screens|Where-Object{[string]$_.status -eq "ACTIVE"}).Count;$open=@($screens|Where-Object{[string]$_.status -in @("PLACEHOLDER","PLANNED","PARTIAL")}).Count;Show-EngHeader "UX REGISTRY" "1.2.1 · ux_registry" "ACTIVE" 35 "Panel resumido del registro activo";Write-Host ("Registry: "+(Join-Path $script:EngV2Root "contracts\SCREEN_REGISTRY.json"));Write-Host ("Pantallas: "+$screens.Count+" | ACTIVE: "+$active+" | por trabajar: "+$open);Write-Host "";Write-Host "ESTADOS:";foreach($g in $groups){Write-Host (" - "+$g.Name+": "+$g.Count)};Write-Host "";Write-Host "AREAS:";$shown=@($roots|Select-Object -First 8);foreach($s in $shown){Write-Host (" - "+$s.level+" · "+$s.title+"  ["+$s.status+" · "+$s.progress+"%]")};if($roots.Count -gt 8){Write-Host (" - +"+($roots.Count-8)+" areas mas")};Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones";Write-Host " [v] Reglas visuales";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"v"{Show-EngLayoutRulesScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Panel resumido. Las areas se abren desde el menu raiz.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   18: function Show-EngScreenStatesScreen{while($true){$screens=Get-EngScreens;$groups=$screens|Group-Object status|Sort-Object Name;Show-EngHeader "ESTADOS DE PANTALLA" "1.2.2 · screen_states" "ACTIVE" 35 "Resumen por estado";foreach($g in $groups){Write-Host (" "+$g.Name+" ("+$g.Count+")") -ForegroundColor Cyan;$sample=@($g.Group|Sort-Object {Get-EngLevelSortKey $_}|Select-Object -First 5);foreach($s in $sample){Write-Host ("   - "+$s.level+" "+$s.title+" ["+$s.progress+"%]")};if($g.Count -gt 5){Write-Host ("   - +"+($g.Count-5)+" mas")};Write-Host ""};Write-Host "SIGUIENTE:";Write-Host " - Priorizar PLACEHOLDER y PLANNED antes de ampliar menus.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Agrupa pantallas por estado.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   19: function Show-EngLayoutRulesScreen{while($true){Show-EngHeader "REGLAS VISUALES" "1.2.3 · layout_rules" "PLANNED" 20 "Reglas simples de lectura";Write-Host "REGLAS:";Write-Host " - Una pantalla = una decision clara.";Write-Host " - Menu corto: maximo 5 opciones visibles.";Write-Host " - Listas largas solo como resumen + accion.";Write-Host " - Footer corto y comun.";Write-Host " - Context Guardian obligatorio antes de parchear.";Write-Host "";Write-Host "SIGUIENTE:";Write-Host " - Convertir reglas en validador automatico.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Reglas UX actuales del operador.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   20: function Show-EngPendingUxRepairsScreen{while($true){$screens=Get-EngScreens;$items=@();foreach($s in ($screens|Sort-Object {Get-EngLevelSortKey $_})){foreach($p in @($s.pending)){$items+=[pscustomobject]@{level=$s.level;title=$s.title;pending=$p;status=$s.status}}};Show-EngHeader "REPARACIONES UX" "1.2.4 · pending_ux_repairs" "ACTIVE" 35 "Pendientes priorizados";Write-Host ("Pendientes totales: "+$items.Count);Write-Host "";Write-Host "TOP 8:";foreach($x in ($items|Select-Object -First 8)){Write-Host (" - "+$x.level+" "+$x.title+": "+$x.pending)};if($items.Count -gt 8){Write-Host (" - +"+($items.Count-8)+" pendientes mas")};Write-Host "";Write-Host "GUARDIAN:";Write-Host " - active_root debe coincidir antes de parchear.";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Lista corta de reparaciones UX.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function Show-EngProgressPendingScreen{while($true){$screens=Get-EngScreens;$total=$screens.Count;$avg=0;if($total -gt 0){$avg=[math]::Round((($screens|ForEach-Object{[int]$_.progress}|Measure-Object -Average).Average),2)};$pendingCount=0;foreach($s in $screens){$pendingCount+=@($s.pending).Count};Show-EngHeader "PROGRESO Y PENDIENTES" "1.3 · progreso_pendientes" "ACTIVE" 25 "Resumen operativo";Write-Host ("Pantallas: "+$total);Write-Host ("Progreso medio: "+$avg+"%");Write-Host ("Pendientes: "+$pendingCount);Write-Host "";Write-Host "ACCIONES:";Write-Host " [e] Estados";Write-Host " [r] Reparaciones UX";Write-Host " [x] Registry";Show-EngFooter;$cmd=Read-EngCmd;switch($cmd){"refresh"{continue}"b"{return}"q"{return}"e"{Show-EngScreenStatesScreen;continue}"r"{Show-EngPendingUxRepairsScreen;continue}"x"{Show-EngUxRegistryScreen;continue}"m"{Show-EngMaintenance;continue}"?"{Write-Host "Resumen global de progreso.";Start-Sleep -Seconds 1;continue}default{Write-Host "Opcion no reconocida" -ForegroundColor Yellow;Start-Sleep -Milliseconds 700}}}}
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 3 score 5 why nav_tokens parse_ok True
```powershell
    1: $script:EngV2Root = "C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2"
    2: $script:EngCoreRoot = "C:\ENGREMIAT_CORE"
    3: $script:EngFooter = "[b/q] salir/volver | m = mantenimiento | ? = ayuda | Enter = refrescar"
    4: $script:EngPrompt = "ENGREMIAT_V2"
    5: function Read-EngJson { param([string]$Path) try { if(Test-Path -LiteralPath $Path){ return Get-Content -LiteralPath $Path -Raw | ConvertFrom-Json } } catch {} return $null }
    6: function Get-EngState { $obs=Read-EngJson (Join-Path $script:EngCoreRoot "reports\observer\latest-observer.json"); $watch=Read-EngJson (Join-Path $script:EngCoreRoot "memory\development\watch-state.json"); return [ordered]@{ observer_status = if($obs){[string]$obs.status}else{"NO_LATEST"}; observer_needs = if($obs){@($obs.needs).Count}else{-1}; watch_status = if($watch){[string]$watch.status}else{"NO_STATE"}; watch_changes = if($watch){[int]$watch.changes_count}else{-1}; watch_tracked = if($watch){[int]$watch.tracked_files}else{-1}; core_root = $script:EngCoreRoot; v2_root = $script:EngV2Root } }
    7: function Show-EngHeader { param([string]$Title,[string]$Route,[string]$Purpose) Clear-Host; Write-Host ("==== ENGREMIAT / " + $Title + " ====") -ForegroundColor Cyan; Write-Host ""; Write-Host "Modo: paralelo / no rompe operador actual"; Write-Host ("Core: " + $script:EngCoreRoot); if($Route){ Write-Host ("Ruta: " + $Route) }; if($Purpose){ Write-Host ("Proposito: " + $Purpose) }; $s=Get-EngState; Write-Host ""; Write-Host ("Observer: " + $s.observer_status + " | needs=" + $s.observer_needs); Write-Host ("Watch: " + $s.watch_status + " | changes=" + $s.watch_changes + " | tracked=" + $s.watch_tracked); Write-Host "" }
    8: function Show-EngFooter { Write-Host ""; Write-Host $script:EngFooter -ForegroundColor Gray; Write-Host "" }
    9: function Show-EngMenu { param([array]$Items) Write-Host "MENU"; foreach($it in $Items){ Write-Host (" [" + $it.key + "] " + $it.label) }; Show-EngFooter }
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\operator\lib\OperatorV2.UI.ps1 line 48 score 5 why nav_tokens parse_ok True
```powershell
   44:   Pause-Eng
   45: }
   46: 
   47: function Show-EngMaintenanceV2 {
   48:   Show-EngHeader "MANTENIMIENTO V2" "maintenance" "Herramientas locales del operador paralelo"
   49:   Write-Host "Comandos disponibles:"
   50:   Write-Host " - status: pantalla principal del operador"
   51:   Write-Host " - observer: usar obs.ps1 desde CORE si hace falta"
   52:   Write-Host " - timeline: abrir memoria de desarrollo"
   53:   Write-Host ""
   54:   Write-Host "Rutas V2:"
```
### C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-engine.ps1 line 5 score 4 why fallback parse_ok True
```powershell
    1: $ErrorActionPreference = 'Stop'
    2: $script:EngScreenActionsReaderPath = 'C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\engine\screen-actions-reader.ps1'
    3: if(!(Test-Path $script:EngScreenActionsReaderPath)){ throw "Reader no encontrado: $script:EngScreenActionsReaderPath" }
    4: . $script:EngScreenActionsReaderPath
    5: function Get-EngScreenActionsDefaultContractPath {
    6:   return 'C:\Users\pc\Desktop\ENGREMIAT_OPERADOR_V2\contracts\SCREEN_ACTIONS.json'
    7: }
    8: function Get-EngScreenActionsEngineState {
    9:   param([string]$ContractPath)
   10:   if([string]::IsNullOrWhiteSpace($ContractPath)){ $ContractPath = Get-EngScreenActionsDefaultContractPath }
   11:   $contract = Read-EngScreenActionsContract -Path $ContractPath
```

## Siguiente
E02 crear contrato COMMAND_FEEDBACK.json con mensajes canonicos y politica dry-run.
