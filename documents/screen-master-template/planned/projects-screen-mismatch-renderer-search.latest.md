# E19B projects screen mismatch and renderer search

## Decision
GO_PROJECTS_SCREEN_RENDERER_FOUND_NO_APPLY

## Manual finding
La pantalla visible es PROYECTOS / INICIO > Proyectos, no PROYECTOS ACTIVOS. Falta m, ?, q.

## Best candidate
- path: C:\ENGREMIAT_CORE\tools\launcher\ENGREMIAT-PROJECTS-CENTER-NORMALIZED.ps1
- score: 5
- matched: ==== PROYECTOS ==== | Ruta: INICIO > Proyectos | Rol: cartera de proyectos | Principio: PROYECTO > MODULO > TARJETA | numero = abrir proyecto | b = volver | Enter = refrescar

## Hits
| score | readhosts | m | ? | q | parse | path | matched |
|---:|---:|---|---|---|---|---|---|
| 5 | 7 | False | False | False | True | C:\ENGREMIAT_CORE\tools\launcher\ENGREMIAT-PROJECTS-CENTER-NORMALIZED.ps1 | ==== PROYECTOS ==== | Ruta: INICIO > Proyectos | Rol: cartera de proyectos | Principio: PROYECTO > MODULO > TARJETA | numero = abrir proyecto | b = volver | Enter = refrescar |
| 2 | 2 | True | True | True | True | C:\ENGREMIAT_CORE\documents\screen-normalizer-internal-candidates-generation\generated\PROYECTOS.candidate.isolated.ps1 | ==== PROYECTOS ==== | Ruta: INICIO > Proyectos |
| 2 | 4 | False | False | True | True | C:\ENGREMIAT_CORE\tools\launcher\ENGREMIAT-CARDS-VIEW-NORMALIZED.ps1 | Ruta: INICIO > Proyectos | Principio: PROYECTO > MODULO > TARJETA |
| 1 | 40 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\contextual-m-card-command-patch.latest.md | PROYECTOS: |
| 1 | 13 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.contextual-m-card-selected.latest.ps1 | PROYECTOS: |
| 1 | 13 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.fix-bq-return.latest.ps1 | PROYECTOS: |
| 1 | 13 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.maintenance-card-cancel.latest.ps1 | PROYECTOS: |
| 1 | 18 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.m-guard-after-readhost-selected.latest.ps1 | PROYECTOS: |
| 1 | 13 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.multiline-stable-router-with-m.bis.latest.ps1 | PROYECTOS: |
| 1 | 13 | True | True | True | False | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.multiline-stable-router-with-m.latest.ps1 | PROYECTOS: |
| 1 | 18 | True | True | True | True | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\ENGREMIAT-INICIO-NORMALIZED.real-inicio-m-route-selected.latest.ps1 | PROYECTOS: |
| 1 | 198 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\inicio-main-router-readhost-deep-audit.latest.md | PROYECTOS: |
| 1 | 242 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\inicio-nonempty-command-router-audit.latest.md | PROYECTOS: |
| 1 | 18 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\inicio-router-direct-lines-25-130.latest.md | PROYECTOS: |
| 1 | 117 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\inicio-router-readhost-context-map.latest.md | PROYECTOS: |
| 1 | 25 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\inicio-router-ultra-stable-audit.latest.md | PROYECTOS: |
| 1 | 73 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\m-command-contextual-card-entry-audit.latest.md | PROYECTOS: |
| 1 | 124 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\m-guard-after-readhost-patch.latest.md | PROYECTOS: |
| 1 | 170 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\m-route-order-command-not-recognized-audit.latest.md | PROYECTOS: |
| 1 | 13 | True | True | True |  | C:\ENGREMIAT_CORE\documents\manual-screen-maintenance-cards\planned\real-inicio-m-route-patch.latest.md | PROYECTOS: |
| 1 | 0 | False | False | False |  | C:\ENGREMIAT_CORE\documents\operator-navigation\operator-navigation-model.md | PROYECTOS: |
| 1 | 30 | True | True | False |  | C:\ENGREMIAT_CORE\documents\operator-real-screen-context-visual-smoke\diagnostics\real-inicio-screen-source-strict.latest.json | PROYECTOS: |
| 1 | 30 | True | True | False |  | C:\ENGREMIAT_CORE\documents\operator-real-screen-context-visual-smoke\diagnostics\real-inicio-screen-source-strict-20260620-161650.json | PROYECTOS: |
| 1 | 17 | False | False | False |  | C:\ENGREMIAT_CORE\documents\screen-master-template\planned\e13b-rollback-eng-context-after-manual-fail.latest.md | PROYECTOS: |
| 1 | 15 | True | True | True | True | C:\ENGREMIAT_CORE\documents\screen-master-template\planned\eng-context.screen-master-candidate.latest.ps1 | PROYECTOS: |
| 1 | 8 | True | True | True | True | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\ENGREMIAT-INICIO-NORMALIZED.operator-access-selected.latest.ps1 | PROYECTOS: |
| 1 | 9 | True | True | True | True | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\ENGREMIAT-INICIO-NORMALIZED.workers-paused-operator-access-selected.latest.ps1 | PROYECTOS: |
| 1 | 59 | True | True | True |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\human-entrypoints-audit.latest.md | Ruta: INICIO > Proyectos |
| 1 | 8 | True | True | True |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\inicio-operator-access-patch.latest.md | PROYECTOS: |
| 1 | 80 | True | True | True |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\real-inicio-chain-audit.latest.md | PROYECTOS: |
| 1 | 9 | True | True | True |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-general-launcher-menu-router-integration-gate\planned\workers-paused-operator-access-patch.latest.md | PROYECTOS: |
| 1 | 8 | True | True | False |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-target-launcher-disambiguation\audit\target-launcher-disambiguation-audit.latest.json | PROYECTOS: |
| 1 | 8 | True | True | False |  | C:\ENGREMIAT_CORE\documents\screen-normalizer-target-launcher-disambiguation\audit\target-launcher-disambiguation-audit-20260620-172139.json | PROYECTOS: |
| 1 | 0 | False | False | False |  | C:\ENGREMIAT_CORE\documents\ux-system\screens\E51_NEXT_ROOT_HUB_BLOCK.selection.md | PROYECTOS: |
| 1 | 0 | False | False | False |  | C:\ENGREMIAT_CORE\documents\ux-system\screens\OPERATOR_NAVIGATION_MODEL_APPLIED_TO_PROJECTS.json | PROYECTOS: |
| 1 | 8 | False | False | False | True | C:\ENGREMIAT_CORE\tools\desktop-terminal-operator\eng-context.ps1 | PROYECTOS: |
| 1 | 2 | False | False | True | True | C:\ENGREMIAT_CORE\tools\launcher\ENGREMIAT-CARD-DETAIL-VIEW-NORMALIZED.ps1 | Ruta: INICIO > Proyectos |
| 1 | 13 | True | True | True | True | C:\ENGREMIAT_CORE\tools\launcher\ENGREMIAT-INICIO-NORMALIZED.ps1 | PROYECTOS: |

## Context best candidate
```powershell
### around line 9
    3: $Core='C:\ENGREMIAT_CORE'
    4: $ProjectsDir=Join-Path $Core 'projects'
    5: $LauncherDir=Join-Path $Core 'tools\launcher'
    6: $CardsView=Join-Path $LauncherDir 'ENGREMIAT-CARDS-VIEW-NORMALIZED.ps1'
    7: $SignalsConfig=Join-Path $Core 'documents\worker-sync\worker-signals-config.json'
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}

### around line 10
    4: $ProjectsDir=Join-Path $Core 'projects'
    5: $LauncherDir=Join-Path $Core 'tools\launcher'
    6: $CardsView=Join-Path $LauncherDir 'ENGREMIAT-CARDS-VIEW-NORMALIZED.ps1'
    7: $SignalsConfig=Join-Path $Core 'documents\worker-sync\worker-signals-config.json'
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}

### around line 11
    5: $LauncherDir=Join-Path $Core 'tools\launcher'
    6: $CardsView=Join-Path $LauncherDir 'ENGREMIAT-CARDS-VIEW-NORMALIZED.ps1'
    7: $SignalsConfig=Join-Path $Core 'documents\worker-sync\worker-signals-config.json'
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}

### around line 12
    6: $CardsView=Join-Path $LauncherDir 'ENGREMIAT-CARDS-VIEW-NORMALIZED.ps1'
    7: $SignalsConfig=Join-Path $Core 'documents\worker-sync\worker-signals-config.json'
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}

### around line 13
    7: $SignalsConfig=Join-Path $Core 'documents\worker-sync\worker-signals-config.json'
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}

### around line 14
    8: $LatestSignal=Join-Path $Core 'documents\worker-sync\project-signal-latest.json'
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 15
    9: function W($m,$c='Gray'){try{Write-Host $m -ForegroundColor $c}catch{Write-Host $m}}
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 16
   10: function SafeId($p){$j=Join-Path $p 'project.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.project_id){return [string]$o.project_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $p -Leaf)}
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 17
   11: function WorkersState(){if(Test-Path $SignalsConfig){try{$c=Get-Content $SignalsConfig -Raw|ConvertFrom-Json;if($c.enabled -eq $true){return 'ON'}}catch{}};return 'OFF'}
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 18
   12: function LatestWorkerText(){if((WorkersState) -ne 'ON'){return 'Workers OFF'};if(Test-Path $LatestSignal){try{$s=Get-Content $LatestSignal -Raw|ConvertFrom-Json;return (([string]$s.kind)+' / '+([string]$s.status)+' / '+([string]$s.readiness)+'%')}catch{return 'Signal unreadable'}};return 'Sin senal'}
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 19
   13: function GetProjects(){@(Get-ChildItem $ProjectsDir -Directory -ErrorAction SilentlyContinue|Sort-Object LastWriteTime -Descending)}
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 20
   14: function GetModules($ProjectPath){$m=Join-Path $ProjectPath 'MODULOS';if(Test-Path $m){@(Get-ChildItem $m -Directory -ErrorAction SilentlyContinue|Sort-Object Name)}else{@()}}
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 21
   15: function ModuleId($ModulePath){$j=Join-Path $ModulePath 'module.json';if(Test-Path $j){try{$o=Get-Content $j -Raw|ConvertFrom-Json;if($o.module_id){return [string]$o.module_id};if($o.id){return [string]$o.id};if($o.name){return [string]$o.name}}catch{}};return (Split-Path $ModulePath -Leaf)}
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 22
   16: function OpenPath($p){if(Test-Path $p){Start-Process $p}else{W "NO_GO no existe: $p" Red;Read-Host 'Enter'|Out-Null}}
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

### around line 23
   17: function CardsFor($ProjectId,$ModuleId){if(!(Test-Path $CardsView)){W "NO_GO falta vista tarjetas: $CardsView" Red;Read-Host 'Enter'|Out-Null;return};& powershell -NoProfile -ExecutionPolicy Bypass -File $CardsView -ProjectId $ProjectId -ModuleId $ModuleId}
   18: function Header(){Clear-Host;W '==== PROYECTOS ====' Cyan;W 'Ruta: INICIO > Proyectos' DarkCyan;W 'Rol: cartera de proyectos, modulos y tarjetas vinculadas' DarkCyan;W 'Principio: PROYECTO > MODULO > TARJETA | readonly | workers seguros' DarkCyan;W ''}
   19: function ProjectList(){while($true){Header;$items=GetProjects;if($items.Count -eq 0){W 'No hay proyectos en C:\ENGREMIAT_CORE\projects' Yellow}else{$i=0;foreach($p in $items){$i++;$ProjectIdValue=SafeId $p.FullName;W ('['+$i+'] '+$ProjectIdValue+'  '+$p.Name) White}};W '';W 'numero = abrir proyecto | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){break};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $items.Count){ProjectActions $items[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   20: function ProjectActions($ProjectPath){$ProjectId=SafeId $ProjectPath;while($true){Clear-Host;W '==== PROYECTO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId) DarkCyan;W 'Rol: acciones locales del proyecto' DarkCyan;W 'Principio: sin workers reales, sin git, sin red' DarkCyan;W '';W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W '[1] abrir carpeta del proyecto' White;W '[2] abrir project.json' White;W '[3] modulos' White;W '[4] tarjetas canon navegacion' White;W '[5] estado/ayuda' White;W '[b] volver a cartera | Enter = refrescar' DarkGray;$c=Read-Host 'PROYECTO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ProjectPath}'2'{OpenPath (Join-Path $ProjectPath 'project.json')}'3'{ModulesScreen $ProjectPath $ProjectId}'4'{CardsFor 'ENGREMIAT_CORE' 'MODULE_OPERATOR_NAVIGATION'}'5'{ProjectStatus $ProjectPath $ProjectId}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   21: function ModulesScreen($ProjectPath,$ProjectId){while($true){Clear-Host;W '==== MODULOS ====' Cyan;W ('Ruta: INICIO > Proyectos > '+$ProjectId+' > Modulos') DarkCyan;W 'Rol: modulos del proyecto y tarjetas vinculadas' DarkCyan;W '';$mods=GetModules $ProjectPath;if($mods.Count -eq 0){W 'No hay carpeta MODULOS o no contiene modulos.' Yellow}else{$i=0;foreach($m in $mods){$i++;W ('['+$i+'] '+(ModuleId $m.FullName)+'  '+$m.Name) White}};W '';W 'numero = abrir modulo | b = volver | Enter = refrescar' DarkGray;$c=Read-Host 'MODULOS';if([string]::IsNullOrWhiteSpace($c)){continue};if($c.ToLower() -eq 'b'){return};$n=0;if([int]::TryParse($c,[ref]$n) -and $n -ge 1 -and $n -le $mods.Count){ModuleActions $ProjectId $mods[$n-1].FullName}else{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}
   22: function ModuleActions($ProjectId,$ModulePath){$Mid=ModuleId $ModulePath;while($true){Clear-Host;W '==== MODULO / ACCIONES ====' Cyan;W ('Ruta: INICIO > Proyectos > Proyecto > Modulos > '+$Mid) DarkCyan;W 'Rol: acciones locales del modulo' DarkCyan;W '';W ('Proyecto: '+$ProjectId) Gray;W ('Modulo:   '+$Mid) White;W ('Ruta:     '+$ModulePath) Gray;W '';W '[1] abrir carpeta del modulo' White;W '[2] abrir module.json' White;W '[3] tarjetas del modulo' White;W '[b] volver a modulos | Enter = refrescar' DarkGray;$c=Read-Host 'MODULO';if([string]::IsNullOrWhiteSpace($c)){continue};switch($c.ToLower()){'1'{OpenPath $ModulePath}'2'{OpenPath (Join-Path $ModulePath 'module.json')}'3'{CardsFor $ProjectId $Mid}'b'{return}default{W 'Comando no reconocido' Yellow;Start-Sleep -Milliseconds 700}}}}
   23: function ProjectStatus($ProjectPath,$ProjectId){Clear-Host;W '==== PROYECTO / ESTADO ====' Cyan;W ('Proyecto: '+$ProjectId) White;W ('Ruta: '+$ProjectPath) Gray;W ('Workers: '+(WorkersState)) Yellow;W ('Ultima senal: '+(LatestWorkerText)) Gray;W '';W 'Tarjetas: entrar por [3] modulos > modulo > [3] tarjetas del modulo.' Cyan;W '';W '[Enter] volver' DarkGray;Read-Host|Out-Null}
   24: ProjectList

```

## Next
E20_AUDIT_PROJECTS_SCREEN_ROUTER_NO_APPLY
