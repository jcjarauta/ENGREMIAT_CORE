SECOND REAL FUNCTION SELECTION RATIONALE

This function is intentionally small so the template can be validated on a second real case without adding new structural complexity.

Why it fits:
- Reuses the standard template created in E172-E175.
- Keeps the same local-first and evidence-first constraints.
- Avoids multi-task or external dependencies.
- Tests whether the pattern is generic and reusable.

Selected case:
- SECOND_REAL_FUNCTION_TEMPLATE_REUSE_001
- Validate template reuse on a second minimal local function

Success condition:
- The second function can be scoped, executed and evidenced using the same template pattern without redesigning the workflow.
