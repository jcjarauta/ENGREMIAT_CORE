CORE_TOOLCHAIN_MANUAL_INSTALL

Estado: falta herramienta base para instalación automática.

Faltan detectadas: 
winget, node, npm, git

Acción humana recomendada:
1. Instalar Node.js LTS manualmente o habilitar winget/App Installer.
2. Instalar Git manualmente si falta.
3. Cerrar y reabrir PowerShell para refrescar PATH.
4. Volver a ejecutar la validación E02.

No se ha ejecutado instalación automática.
No se ha hecho git write, commit, push, fetch ni merge.
No se ha ejecutado worker real.
